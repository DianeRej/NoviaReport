@model NoviaReport.Models.CRA
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Ajouter un CRA</title>
</head>
<body>

    @using (Html.BeginForm())
    {
        <fieldset>
            <legend>Ajouter un CRA : </legend>
            @Html.LabelFor(m => Model.Date)
            @Html.TextBoxFor(m=>Model.Date,"{0:yyyy-MM-dd}" ,new{@type="date"})
            <br />
            @*Liste déroulante qui permet de choisir l'état du CRA : inutile puisqu'à
        sa création le CRA va forcément être vide et donc aura le statut NON_VALIDE. A la place on ne remplit pas ce champ mais
        la méthode createCRA applique automatiquement l'état NON_VALIDE au nouveau CRA créé*@

            @*@Html.LabelFor(m => Model.State)
        <select id="State" name="State">
        <option value="">Etat du CRA</option>
        @foreach (var item in Enum.GetValues(typeof(NoviaReport.Models.State)))
        {
        <option value=@item name="State">@item</option>
        }
        </select>*@
            <br />

            <input type="submit" value="Ajouter" />
        </fieldset>
        <a href="/CRA/CreateCRA"></a>
    }

<select>
    @foreach (NoviaReport.Models.TypeActivity actType List<NoviaReport.Models.TypeActivity>)
    {
        <option id=""></option>
    }
</select>
    

</body>
</html>