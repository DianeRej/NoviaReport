@model NoviaReport.Models.Activity
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Add activity</title>
</head>
<body>
   @* <form method="post" action="">
        <table border="1">
            <tr>
                <td>annee</td>
                <td><input name="year" id="year" /></td>
            </tr>
            <tr>
                <td>mois</td>
                <td><input name="month" id="month" /></td>
            </tr>
            <tr>
                <td>jour</td>
                <td><input name="day" id="day" /></td>
            </tr>
            <tr>
                <td>activité</td>
                <td><input name="activity" id="activity" /></td>
            </tr>
            <tr>
                <td colspan="2"><button type="submit" value="ajouter" /></td>
            </tr>
        </table>
    </form>
*@
    @using (Html.BeginForm())
    {
        <fieldset>
            <legend>Ajouter une activité : </legend>
            @Html.LabelFor(m => Model.Date)
            @Html.TextBoxFor(m => Model.Date)
            @Html.ValidationMessageFor(m => Model.Date)
            <br />
            @Html.LabelFor(m => Model.TypeActivity)
            @Html.TextBoxFor(m => Model.TypeActivity)
            @Html.ValidationMessageFor(m => Model.TypeActivity)
            <br />
            <input type="submit" value="Ajouter" />
        </fieldset>
        <a href="/CRA/CreateActivity"></a>
    }

</body>
</html>
